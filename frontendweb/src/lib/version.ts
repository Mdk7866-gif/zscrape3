/**
 * Single source of truth for the version string.
 *
 * This used to be duplicated — a const in app/page.tsx and a hardcoded literal
 * in Navbar.tsx, with a "keep these in sync" comment on both. Now there's one.
 */
export const APP_VERSION = "18.08.2026V2";
